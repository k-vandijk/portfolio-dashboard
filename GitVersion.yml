mode: ContinuousDelivery
branches:
  main:
    regex: ^main$
    increment: Patch
    is-main-branch: true
  dev:
    regex: ^dev$
    increment: Minor
  feature:
    regex: ^feat-
    increment: Minor
  fix:
    regex: ^fix-
    increment: Patch
ignore:
  sha: []